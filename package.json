{
  "name": "sapisehat",
  "version": "1.0.0",
  "description": "SapiSehat monorepo — cattle disease detection app",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=18",
    "pnpm": ">=10"
  },
  "scripts": {
    "backend:build": "cd apps/backend && docker compose build",
    "backend:dev": "cd apps/backend && docker compose down -v && docker compose up --build -d",
    "backend:test": "cd apps/backend && docker compose run --rm backend python -m pytest tests/ -v",
    "backend:up": "cd apps/backend && docker compose up --build -d",
    "backend:down": "cd apps/backend && docker compose down",
    "backend:logs": "cd apps/backend && docker compose logs -f",
    "dashboard:dev": "pnpm --dir apps/dashboard dev",
    "dashboard:build": "pnpm --dir apps/dashboard build",
    "dashboard:start": "pnpm --dir apps/dashboard start",
    "dashboard:lint": "pnpm --dir apps/dashboard lint",
    "dashboard:typecheck": "pnpm --dir apps/dashboard exec tsc --noEmit",
    "mobile:build": "cd apps/mobile && flutter build apk --debug",
    "mobile:deploy": "pnpm run mobile:build && node scripts/run-adb.mjs install -r apps/mobile/build/app/outputs/flutter-apk/app-debug.apk",
    "mobile:run": "node scripts/run-flutter-first.mjs run",
    "mobile:restart": "node scripts/run-adb.mjs shell am force-stop id.sapisehat.sapisehat_mobile && node scripts/run-adb.mjs shell am start -n id.sapisehat.sapisehat_mobile/.MainActivity",
    "mobile:log": "node scripts/run-adb.mjs logcat | findstr /i \"flutter sapisehat\"",
    "mobile:test": "cd apps/mobile && flutter test",
    "mobile:clean": "cd apps/mobile && flutter clean",
    "mobile:pub": "cd apps/mobile && flutter pub get",
    "dev": "pnpm run backend:dev",
    "test": "pnpm run backend:test",
    "build": "pnpm run backend:build && pnpm run dashboard:build",
    "install:all": "pnpm run backend:build"
  }
}
