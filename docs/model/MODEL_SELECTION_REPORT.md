# Model Selection Report (issue #21)

Generated by `docs/model/model_selection.py` from real candidate evaluation results.

## Selection Rules (issue #17)

1. **Reject** any candidate with per-class F1 < 0.85 (unless documented exception).
2. **Primary metric**: macro F1 (highest wins).
3. **Accuracy target**: test accuracy ≥ 0.88.
4. **Tie-breakers** (macro F1 within 0.005): test accuracy > TFLite size < offline latency.
5. **Android viability**: TFLite size < 10.0 MB for on-device candidate.
6. **Failure policy**: if no candidate reaches targets, mark best as experimental/demo with follow-up.

## Candidate Results

| Model | Accuracy | Macro F1 | Min per-class F1 | TFLite est. | Status |
|-------|----------|----------|-------------------|------------|--------|
| custom_cnn | 0.912 | 0.916 | 0.904 (LSD) | ~0.5 MB | ✅ passed |
| densenet121 | 0.993 | 0.995 | 0.991 (healthy) | ~30.0 MB | ✅ passed |
| mobilenetv2 | 0.970 | 0.972 | 0.967 (LSD) | ~9.0 MB | ⭐ selected |

## Decision

**Status**: selected

**Rationale**: mobilenetv2 selected: macro F1=0.972, accuracy=0.970, min per-class F1=0.967 (LSD), TFLite ~9.0 MB. Meets all targets and Android viability constraint.

### Selected model details

| Property | Value |
|----------|-------|
| Model | mobilenetv2 |
| Test accuracy | 0.9697 |
| Macro F1 | 0.9718 |
| Min per-class F1 | 0.9668 (LSD) |
| Parameters | 2,422,339 |
| Keras size | 22.72 MB |
| TFLite size (est.) | ~9.0 MB |
| Evaluated on | test split |
| Android viable | ✅ Yes |

### Server Inference

The selected model (mobilenetv2) is deployed as a Keras/SavedModel artifact on the backend server (FastAPI + PyTorch/TF Serving). It provides the highest accuracy among Android-viable candidates while meeting all per-class F1 and accuracy targets.

### On-device Inference

The same architecture is exported to TFLite (~9.0 MB, within the 10.0 MB budget) for offline fallback on Android. This ensures dual-path consistency: the same class mapping, preprocessing, and model family serve both server and mobile inference paths.

## Warnings

- densenet121: TFLite size ~30.0 MB exceeds 10.0 MB budget. Not viable for on-device inference. Consider the next-best Android-viable candidate.
- Falling back to mobilenetv2 (TFLite ~9.0 MB) for dual-path (server + on-device) deployment.

## Failure Policy

✅ The selected candidate meets all targets (accuracy ≥ 0.88, per-class F1 ≥ 0.85, TFLite < 10.0 MB). No failure policy triggered.

## Wording Compliance

Per issue #17, all thesis/app/report outputs frame results as:

- Image classification / early detection result
- Prediction with confidence score
- Initial handling advice

**NOT** as: final diagnosis, veterinarian replacement, or clinical disease determination.

## Limitations

- Single-seed evaluation (seed 42). Winner should be rerun with additional seeds for robustness.
- TFLite sizes are estimates until actual conversion (issue #22).
- No veterinarian/animal-health-officer label audit performed.
- Not a clinical diagnosis tool.
