info:
  name: GET farmers by farmer_id follow-ups
  type: http
  seq: 10
  tags:
    - Farmers

http:
  method: GET
  url: "{{base_url}}/api/farmers/{{farmer_id}}/follow-ups"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  ### List Farmer Follow Up Status

  `GET /api/farmers/{farmer_id}/follow-ups`

  Source: `apps/backend/api/routes.py:815`
