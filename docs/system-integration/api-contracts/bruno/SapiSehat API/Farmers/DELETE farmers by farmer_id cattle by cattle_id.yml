info:
  name: DELETE farmers by farmer_id cattle by cattle_id
  type: http
  seq: 7
  tags:
    - Farmers

http:
  method: DELETE
  url: "{{base_url}}/api/farmers/{{farmer_id}}/cattle/{{cattle_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  ### Archive Farmer Cattle

  `DELETE /api/farmers/{farmer_id}/cattle/{cattle_id}`

  Source: `apps/backend/api/routes.py:449`
