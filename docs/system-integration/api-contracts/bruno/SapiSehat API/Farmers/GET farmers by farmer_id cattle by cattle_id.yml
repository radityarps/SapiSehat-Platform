info:
  name: GET farmers by farmer_id cattle by cattle_id
  type: http
  seq: 6
  tags:
    - Farmers

http:
  method: GET
  url: "{{base_url}}/api/farmers/{{farmer_id}}/cattle/{{cattle_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  ### Select Farmer Cattle For Detection

  `GET /api/farmers/{farmer_id}/cattle/{cattle_id}`

  Source: `apps/backend/api/routes.py:441`
