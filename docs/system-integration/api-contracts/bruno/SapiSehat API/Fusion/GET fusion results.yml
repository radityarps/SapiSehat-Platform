info:
  name: GET fusion results
  type: http
  seq: 2
  tags:
    - Fusion

http:
  method: GET
  url: "{{base_url}}/api/fusion/results"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  ### List Fusion Results

  `GET /api/fusion/results`

  Source: `apps/backend/api/routes.py:613`
