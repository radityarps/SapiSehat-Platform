info:
  name: POST auth agency google
  type: http
  seq: 5
  tags:
    - Auth

http:
  method: POST
  url: "{{base_url}}/api/auth/agency/google"
  auth: inherit
  body:
    type: json
    json: |-
      {}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  ### Reject Agency Google Login

  `POST /api/auth/agency/google`

  Source: `apps/backend/api/routes.py:158`
