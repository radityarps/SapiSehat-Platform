info:
  name: POST auth agency login
  type: http
  seq: 4
  tags:
    - Auth

http:
  method: POST
  url: "{{base_url}}/api/auth/agency/login"
  body:
    type: json
    data: |-
      {
              "email": "semarang-officer@sapisehat.test",
              "password": "agency-password"
            }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  ### Login Agency Surface Account

  `POST /api/auth/agency/login`

  Source: `apps/backend/api/routes.py:149`
