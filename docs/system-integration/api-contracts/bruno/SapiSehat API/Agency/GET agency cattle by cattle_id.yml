info:
  name: GET agency cattle by cattle_id
  type: http
  seq: 4
  tags:
    - Agency

http:
  method: GET
  url: "{{base_url}}/api/agency/cattle/{{cattle_id}}"
  auth: inherit
  headers:
    - name: X-Agency-User-Id
      value: "{{agency_user_id}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  ### Get Agency Visible Cattle Detail

  `GET /api/agency/cattle/{cattle_id}`

  Source: `apps/backend/api/routes.py:495`
