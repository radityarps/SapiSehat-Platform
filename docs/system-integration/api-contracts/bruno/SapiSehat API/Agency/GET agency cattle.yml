info:
  name: GET agency cattle
  type: http
  seq: 3
  tags:
    - Agency

http:
  method: GET
  url: "{{base_url}}/api/agency/cattle"
  auth: inherit
  headers:
    - name: X-Agency-User-Id
      value: "{{agency_user_id}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  ### List Agency Visible Cattle

  `GET /api/agency/cattle`

  Source: `apps/backend/api/routes.py:458`
