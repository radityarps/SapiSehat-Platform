info:
  name: GET agency audit-logs
  type: http
  seq: 1
  tags:
    - Agency

http:
  method: GET
  url: "{{base_url}}/api/agency/audit-logs"
  auth: inherit
  headers:
    - name: X-Agency-User-Id
      value: "{{agency_user_id}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  ### List Agency Audit Logs

  `GET /api/agency/audit-logs`

  Source: `apps/backend/api/routes.py:104`
