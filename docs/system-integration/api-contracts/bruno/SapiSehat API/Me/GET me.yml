info:
  name: GET me
  type: http
  seq: 1
  tags:
    - Me

http:
  method: GET
  url: "{{base_url}}/api/me"
  auth: inherit
  headers:
    - name: Authorization
      value: "Bearer {{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  ### Get Current Surface Account

  `GET /api/me`

  Source: `apps/backend/api/routes.py:164`
